#include <stdio.h>

/**
 * main - Entry point
 * print single digit numbers base 10
 * Return: Always 0(Success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		putchar(97+n);

	putchar('\n');
	return (0);
}
