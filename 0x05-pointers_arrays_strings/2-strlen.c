#include "main.h"

/**
 * _strlen - Entry Point
 * Description: swap 2 integers
 * @s: string to find length of
 *
 * Return: string length
 */
int _strlen(char *s)
{
	int len;

	len = sizeof(&s) / sizeof(s);
	return (len);
}
