#include "main.h"
/**
 * _abs - Entry point
 * Description - determine absolute value
 * @c: interger being checked
 * Return: y = absolute value
 */
int _abs(int c)
{
	int y;

	if (c < 0)
	{
		y = -c;
	}
	else
	{
		y = c;
	}
	return (c);
}
