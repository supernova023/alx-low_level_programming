#include "main.h"
/**
 * main - Entry point
 * print out alphabet
 *
 * Return: Always 0(Success)
 */
int main(void)
{
	char x;
	for (x = 'a'; x <= 'z'; x++)
	{
		_putchar(x);
	}
	_putchar('\n');
	return (0);
}
