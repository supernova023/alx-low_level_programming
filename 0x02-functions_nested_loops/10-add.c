#include "main.h"
/**
 * add - Entry point
 * Description - determine sum
 * @a: first integer being added
 * @b: second integer being added
 * Return: y = sum
 */
int add(int a, int b)
{
	int y;

	y = a + b;
	return (y);
}
