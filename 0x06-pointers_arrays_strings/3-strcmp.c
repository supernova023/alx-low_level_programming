#include "main.h"
/**
 * _strcmp - Entry point
 *
 * @s1: variable storing first string to compare with
 * @s2: variable storing second string to compare with
 * Return: resulting comparison value
 */
int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);

	return (res);

}
