#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 *
 * @argv: store all args
 * Return: exit condition if code is successful or not
 */


int main(char **argv)
{
	printf("%s\n", *argv);
	exit(EXIT_SUCCESS);
}
